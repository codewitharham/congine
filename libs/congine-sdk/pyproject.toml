[project]
name = "congine-sdk"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
    "pytest>=9.0.3",
    "ruff>=0.15.15",
    "httpx>=0.28",
    "jsonschema>=4.23",
]

[tool.uv]
package = true
