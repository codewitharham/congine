{
  "name": "congine-sdk",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/congine-sdk/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "uv run --package congine-sdk ruff format --check libs/congine-sdk",
          "uv run --package congine-sdk ruff check libs/congine-sdk"
        ],
        "parallel": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run --package congine-sdk --extra langchain --extra stats pytest libs/congine-sdk/tests"
      }
    }
  }
}
